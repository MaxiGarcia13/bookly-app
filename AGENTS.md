# Agents

Expo SDK **57** · React Native · NativeWind v4 · Expo Router.

## Non-negotiables

1. **Versioned Expo docs only** — https://docs.expo.dev/versions/v57.0.0/ (fetch `.md` pages; map: `llms.txt`). Never unversioned / “latest”.
2. **Skills over reinvention** — project how-tos live in `.cursor/skills/`. Read and follow them; do not restate those rules in new files or long comments.

| Skill           | Use for                                                   |
| --------------- | --------------------------------------------------------- |
| `expo-sdk-57`   | Packages, layout, `@/` aliases, TS / lint / scripts       |
| `nativewind`    | NativeWind / Tailwind / Metro / `global.css` setup        |
| `ui-components` | className rules, variants, shared UI in `src/components/` |
| `expo-router`   | Screens & layouts under `src/app/`                        |

Install Expo packages with `npx expo install`.
